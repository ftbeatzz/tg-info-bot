import os
import random
from aiogram import Bot, Dispatcher, types, F
from aiogram.filters import CommandStart
from aiogram.types import ReplyKeyboardMarkup, KeyboardButton, ReplyKeyboardRemove, FSInputFile
from dotenv import load_dotenv
import asyncio

load_dotenv()

# Получаем токен из переменных окружения
BOT_TOKEN = os.getenv('BOT_TOKEN')

if not BOT_TOKEN:
    raise ValueError("BOT_TOKEN не найден!")

bot = Bot(token=BOT_TOKEN)
dp = Dispatcher()

# Хранение последней использованной картинки
last_random_pic = None

# Хранение контекста пользователя (партнёр/инвестор)
user_context = {}


def get_random_pic():
    global last_random_pic
    pics = ['media/randomPic1.png', 'media/randomPic2.png', 'media/randomPic3.png']
    available_pics = [pic for pic in pics if pic != last_random_pic]
    selected_pic = random.choice(available_pics)
    last_random_pic = selected_pic
    return selected_pic


def get_main_menu():
    keyboard = ReplyKeyboardMarkup(
        keyboard=[
            [KeyboardButton(text="Партнёру")],
            [KeyboardButton(text="Инвестору")]
        ],
        resize_keyboard=True,
        input_field_placeholder="Выберите опцию..."
    )
    return keyboard


def get_back_menu():
    keyboard = ReplyKeyboardMarkup(
        keyboard=[
            [KeyboardButton(text="◀️ Назад"), KeyboardButton(text="🏠 Меню")]
        ],
        resize_keyboard=True,
        input_field_placeholder="Выберите опцию..."
    )
    return keyboard


def get_investor_menu():
    keyboard = ReplyKeyboardMarkup(
        keyboard=[
            [KeyboardButton(text="Как сделать депозит?")],
            [KeyboardButton(text="Как начать работу в пуле?")],
            [KeyboardButton(text="Что такое авто-выплаты?")],
            [KeyboardButton(text="Как сохранить адрес?")],
            [KeyboardButton(text="Об арбитражной модели")],
            [KeyboardButton(text="🎥 Видео-материалы"), KeyboardButton(text="🏠 Меню")]
        ],
        resize_keyboard=True,
        input_field_placeholder="Выберите опцию..."
    )
    return keyboard


def get_partner_menu():
    keyboard = ReplyKeyboardMarkup(
        keyboard=[
            [KeyboardButton(text="Лично-приглашённые")],
            [KeyboardButton(text="Структура")],
            [KeyboardButton(text="Кэшбэк")],
            [KeyboardButton(text="Ранговая система")],
            [KeyboardButton(text="Оффлайн-мероприятие")],
            [KeyboardButton(text="Затраты на маркетинг")],
            [KeyboardButton(text="◀️ Назад"), KeyboardButton(text="🎬 Видео-материалы")]
        ],
        resize_keyboard=True,
        input_field_placeholder="Выберите опцию..."
    )
    return keyboard


def get_partner_video_menu():
    keyboard = ReplyKeyboardMarkup(
        keyboard=[
            [KeyboardButton(text="Вывод из партнёрской системы")],
            [KeyboardButton(text="◀️ Назад"), KeyboardButton(text="🏠 Меню")]
        ],
        resize_keyboard=True,
        input_field_placeholder="Выберите опцию..."
    )
    return keyboard


def get_video_menu():
    keyboard = ReplyKeyboardMarkup(
        keyboard=[
            [KeyboardButton(text="Как начать работу?")],
            [KeyboardButton(text="Как совершить партнёрский вывод?")],
            [KeyboardButton(text="◀️ Назад"), KeyboardButton(text="🏠 Меню")]
        ],
        resize_keyboard=True,
        input_field_placeholder="Выберите опцию..."
    )
    return keyboard


def get_video_investor_menu():
    keyboard = ReplyKeyboardMarkup(
        keyboard=[
            [KeyboardButton(text="Как начать работу?")],
            [KeyboardButton(text="Как совершить партнёрский вывод?")],
            [KeyboardButton(text="◀️ Назад"), KeyboardButton(text="🏠 Меню")]
        ],
        resize_keyboard=True,
        input_field_placeholder="Выберите опцию..."
    )
    return keyboard


def get_video_partner_menu():
    keyboard = ReplyKeyboardMarkup(
        keyboard=[
            [KeyboardButton(text="Вывод из партнёрской системы")],
            [KeyboardButton(text="◀️ Назад"), KeyboardButton(text="🏠 Меню")]
        ],
        resize_keyboard=True,
        input_field_placeholder="Выберите опцию..."
    )
    return keyboard


@dp.message(CommandStart())
async def start_handler(message: types.Message):
    welcome_text = f"""💙 <b>Добро пожаловать в ARB Core INFO, {message.from_user.first_name}</b>

<b>ARB Core INFO</b> — это информационный центр арбитражного ресурса <b>ARB Core</b>, созданный для тех, кто рассматривает партнёрство или инвестиционное участие в системе арбитражных операций.

<b>Через этого бота вы получите:</b>
📊 структурированную информацию о модели арбитража
🎥 видео-материалы с объяснением процессов
📄 прозрачное описание возможностей
🔐 понимание принципов работы

<b>Выберите интересующий вас формат взаимодействия:</b>

🤝 <b>Партнёру</b> — если вы являетесь или планируете стать активным партнёром экосистемы.

💼 <b>Инвестору</b> — если вы рассматриваете размещение капитала и хотите разобраться в механике доходности и стратегии ресурса.

⬇️ <i>Нажмите нужную кнопку ниже, чтобы продолжить знакомство с ARB Core.</i>"""
    
    photo = FSInputFile("media/welcomePic.png")
    await message.answer_photo(
        photo=photo,
        caption=welcome_text,
        reply_markup=get_main_menu(),
        parse_mode='HTML'
    )


@dp.message(F.text == "Партнёру")
async def partner_handler(message: types.Message):
    user_context[message.from_user.id] = "partner"
    loading_msg = await message.answer("Загружаем содержимое...")
    
    partner_text = """🤝 <b>Партнёру | ARB Core INFO</b>

<b>ARB Club</b> — это партнёрская экосистема ресурса <b>ARB Core</b>, ориентированная на развитие сообщества и распределение вознаграждений.

<b>В этом разделе вы получите:</b>
👥 информацию о партнёрской программе
📊 описание структуры и уровней
💰 механику кэшбэка и вознаграждений
🏆 ранговую систему развития
🎯 информацию о мероприятиях и маркетинговой поддержке

⬇️ <i>Выберите интересующий вас раздел ниже.</i>"""
    
    photo = FSInputFile("media/partnerPic.png")
    await message.answer_photo(
        photo=photo,
        caption=partner_text,
        reply_markup=get_partner_menu(),
        parse_mode='HTML'
    )
    
    await loading_msg.delete()


@dp.message(F.text == "Лично-приглашённые")
async def personally_invited_handler(message: types.Message):
    loading_msg = await message.answer("Загружаем содержимое...")
    
    personally_invited_text = """🤝 <b>Вознаграждения от лично приглашённых партнёров</b>

<b>Лично приглашённые партнёры</b> — это пользователи, которые зарегистрировались непосредственно по вашей реферальной ссылке.

За таких партнёров предусмотрено вознаграждение в размере <b>5%</b> от суммы, которую партнёр запускает в арбитражные сделки.

<b>📌 Пример расчёта</b>

Вы пригласили партнёра по своей реферальной ссылке.
Партнёр запустил в арбитраж 10 000 USDT.

🔹 Объём, участвующий в расчёте: 10 000 USDT
🔹 Процент вознаграждения: 5%

💰 <b>Ваше вознаграждение: 500 USDT</b>

<i>Вознаграждение начисляется автоматически после запуска средств партнёра в работу.</i>"""
    
    photo = FSInputFile(get_random_pic())
    await message.answer_photo(
        photo=photo,
        caption=personally_invited_text,
        reply_markup=get_partner_menu(),
        parse_mode='HTML'
    )
    
    await loading_msg.delete()


@dp.message(F.text == "Структура")
async def structure_handler(message: types.Message):
    loading_msg = await message.answer("Загружаем содержимое...")
    
    structure_text = """🌐 <b>Вознаграждения от структуры</b>

Партнёрская структура ARB Core строится по принципу <b>2×2</b> и может достигать <b>21 уровня</b> в глубину.
Открытие уровней происходит в соответствии с ранговой системой.

<b>💼 Принцип начисления вознаграждений</b>

За работу структуры предусмотрено единое вознаграждение:

🔹 <b>10%</b> от дохода партнёров
🔹 процент одинаков для всех уровней
🔹 учитывается именно доход партнёров, а не их депозит

<i>Начисления происходят автоматически в рамках партнёрской системы.</i>

<b>📊 Пример расчёта</b>

Предположим:

• ваш партнёр на одном из уровней структуры получил доход 1 000 USDT;
• ставка вознаграждения от структуры — 10%.

💰 <b>Ваше вознаграждение:</b>
1 000 USDT × 10% = 100 USDT

<i>Если в структуре несколько партнёров на разных уровнях, доход суммируется, и 10% начисляется с общего дохода всех партнёров, входящих в открытую для вас глубину.</i>"""
    
    photo = FSInputFile(get_random_pic())
    await message.answer_photo(
        photo=photo,
        caption=structure_text,
        reply_markup=get_partner_menu(),
        parse_mode='HTML'
    )
    
    await loading_msg.delete()


@dp.message(F.text == "Кэшбэк")
async def cashback_handler(message: types.Message):
    loading_msg = await message.answer("Загружаем содержимое...")
    
    cashback_text = """💸 <b>Кэшбэк</b>

<b>Кэшбэк</b> — это мгновенное вознаграждение пользователя за закрытие торгового объёма в рамках арбитражной модели.

Размер кэшбэка составляет <b>5%</b> от закрытого объёма.

<b>⚙️ Важное условие</b>

Кэшбэк начисляется только за объём, который закрыт в пределах открытого у пользователя ранга.
<i>Объём, превышающий лимит ранга, не участвует в расчёте кэшбэка до открытия следующего ранга.</i>

<b>📊 Пример</b>

У пользователя открыт ранг с лимитом 1 000 USDT.
Пользователь закрыл объём 1 000 USDT.

🔹 Процент кэшбэка: 5%
💰 <b>Кэшбэк: 50 USDT</b>

<i>Начисление происходит мгновенно после закрытия объёма и зачисляется на баланс пользователя.</i>"""
    
    photo = FSInputFile(get_random_pic())
    await message.answer_photo(
        photo=photo,
        caption=cashback_text,
        reply_markup=get_partner_menu(),
        parse_mode='HTML'
    )
    
    await loading_msg.delete()


@dp.message(F.text == "Ранговая система")
async def rank_system_handler(message: types.Message):
    loading_msg = await message.answer("Загружаем содержимое...")
    
    rank_text_1 = """🏅 <b>Ранговая система</b>

Ранговая система определяет доступные лимиты, глубину структуры и объёмы, которые учитываются в работе и вознаграждениях.

Ниже приведён расчётный пример для двух стартовых рангов.

<b>🟤 BRONZE (1–3 уровни)</b>

<b>Условия ранга:</b>
• Личный депозит: 250 USDT
• Мин. активных личных партнёров: 2
• Общий объём личных партнёров: 500 USDT

<b>Возможности ранга:</b>
• Максимум партнёров в структуре (1–3 уровень): 14
• Максимальная сумма вывода в сутки: 1 000 USDT
• В зачёт идут объёмы 1–3 уровней

<b>📊 Пример</b>
Пользователь выполнил условия ранга Bronze и закрыл объём 800 USDT в пределах 1–3 уровней.
<i>В расчётах учитывается только объём, доступный в рамках данного ранга.</i>"""
    
    rank_text_2 = """<b>⚪ SILVER (4–6 уровни)</b>

<b>Условия ранга:</b>
• Личный депозит: 1 000 USDT
• Мин. активных личных партнёров: 2
• Общий объём личных партнёров: 2 500 USDT

<b>Возможности ранга:</b>
• Максимум партнёров в структуре (4–6 уровни): 112
• Максимальная сумма вывода в сутки: 2 000 USDT
• В зачёт идут объёмы 4–6 уровней

<b>📊 Пример</b>
После выполнения условий Silver пользователю открываются уровни 4–6, и объёмы этих уровней начинают учитываться в расчётах и вознаграждениях.

<b>🔎 Важно:</b>
<i>Объёмы, превышающие рамки текущего ранга, не участвуют в расчётах до открытия следующего ранга.</i>"""
    
    photo = FSInputFile(get_random_pic())
    await message.answer_photo(
        photo=photo,
        caption=rank_text_1,
        parse_mode='HTML'
    )
    await message.answer(
        rank_text_2,
        reply_markup=get_partner_menu(),
        parse_mode='HTML'
    )
    
    await loading_msg.delete()


@dp.message(F.text == "Оффлайн-мероприятие")
async def offline_event_handler(message: types.Message):
    loading_msg = await message.answer("Загружаем содержимое...")
    
    offline_text = """🌍 <b>Оффлайн мероприятие ARB Core</b>

В текущий момент планируется оффлайн мероприятие в <b>Гонконге</b> для партнёров, выполнивших условия участия.

<b>📌 Условия участия</b>

Для получения слота на мероприятие необходимо:

🔹 достичь оборота <b>30 000 USDT</b> от лично приглашённых партнёров;
🔹 выполнить условие до момента завершения набора участников.

<i>Подготовка к мероприятию продолжается до формирования группы из 100 участников, выполнивших условия.</i>

<b>🗓 Информация по дате</b>

📢 При достижении <b>80 подтверждённых участников</b> будет официально объявлена дата проведения мероприятия.

<b>👥 Формат участия</b>

🎟 Один слот на мероприятии рассчитан на <b>2 человек</b>.
<i>Вы можете пригласить с собой супруга / супругу или другого сопровождающего.</i>"""
    
    photo = FSInputFile(get_random_pic())
    await message.answer_photo(
        photo=photo,
        caption=offline_text,
        reply_markup=get_partner_menu(),
        parse_mode='HTML'
    )
    
    await loading_msg.delete()


@dp.message(F.text == "Затраты на маркетинг")
async def marketing_costs_handler(message: types.Message):
    loading_msg = await message.answer("Загружаем содержимое...")
    
    marketing_text = """📈 <b>Затраты на маркетинг</b>

Компания использует отдельный режим работы средств — <b>Auto-Earn</b>, направленный на финансирование маркетинга и выполнение задач по дорожной карте проекта.

<b>⚙️ Как работает Auto-Earn:</b>

⏰ Время работы: с <b>22:00 до 09:00 (UTC)</b>

В указанный период средства пользователей могут участвовать в торговле по программе Auto-Earn.

<b>Отличие от стандартного арбитража:</b>

• в обычном режиме арбитража пользователь получает 100% дохода от сделок;
• в режиме Auto-Earn пользователь получает фиксированное вознаграждение.

<b>💰 Условия:</b>

🔹 Пользователь получает <b>0,03%</b> от собственного депозита за каждую сделку, совершённую в режиме Auto-Earn.
🔹 Оставшаяся часть дохода направляется на:

— поддержание маркетинговых активностей;
— операционные расходы компании;
— реализацию дорожной карты проекта."""
    
    photo = FSInputFile(get_random_pic())
    await message.answer_photo(
        photo=photo,
        caption=marketing_text,
        reply_markup=get_partner_menu(),
        parse_mode='HTML'
    )
    
    await loading_msg.delete()


@dp.message(F.text == "Как сделать депозит?")
async def deposit_handler(message: types.Message):
    loading_msg = await message.answer("Загружаем содержимое...")
    
    deposit_text = """💸 <b>Как сделать депозит?</b>

Чтобы пополнить торговый счёт и подготовиться к работе в системе, выполните следующие шаги:

<b>1️⃣ Перейдите в раздел:</b>
Баланс → Пополнить торговый счёт.

<b>2️⃣ Совершите перевод на указанный адрес и MEMO ID, строго соблюдая данные из кабинета:</b>

🔹 <b>Адрес перевода</b>
— скопируйте адрес прямо из личного кабинета;
— вставьте его при переводе с биржи или кошелька.

🔹 <b>MEMO ID</b>
— скопируйте MEMO ID из личного кабинета;
— обязательно укажите его при переводе.
<i>(Отсутствие MEMO может привести к потере средств.)</i>


<b>3️⃣ Отправьте средства и дождитесь зачисления</b>

⏱ Время обработки: от 3 до 7 минут.

<i>После зачисления средства отобразятся на вашем торговом счёте.</i>"""
    
    photo = FSInputFile(get_random_pic())
    await message.answer_photo(
        photo=photo,
        caption=deposit_text,
        reply_markup=get_investor_menu(),
        parse_mode='HTML'
    )
    
    await loading_msg.delete()


@dp.message(F.text == "Как начать работу в пуле?")
async def pool_handler(message: types.Message):
    loading_msg = await message.answer("Загружаем содержимое...")
    
    pool_text = """⚙️ <b>Как начать работу в пуле?</b>

После пополнения баланса выполните следующие шаги:

<b>1️⃣ Нажмите кнопку «Авто-трейдинг» в личном кабинете на странице "Arbitrage".</b>

После этого автоматически происходит:

📄 <b>Формирование смарт-контракта</b>
— контракт фиксирует обязательства компании перед пользователем;
— все дальнейшие расчёты ведутся в рамках данного контракта.

🧾 <b>Создание инвойсов</b>
— по каждому депозиту формируется инвойс;
— инвойсы заверены печатью компании.

🔄 <b>Запуск арбитражной системы</b>
— средства подключаются к торговым алгоритмам;
— начинается работа арбитража.

👥 <b>Включение средств общий пул</b>
— ожидание окончания ближайшей арбитражной сделки;
— депозит поступает в пул пользователей;
— далее средства участвуют в коллективной арбитражной модели."""
    
    photo = FSInputFile(get_random_pic())
    await message.answer_photo(
        photo=photo,
        caption=pool_text,
        reply_markup=get_investor_menu(),
        parse_mode='HTML'
    )
    
    await loading_msg.delete()


@dp.message(F.text == "Что такое авто-выплаты?")
async def autopay_handler(message: types.Message):
    loading_msg = await message.answer("Загружаем содержимое...")
    
    autopay_text = """💸 <b>Что такое Авто-выплаты?</b>

<b>Авто-выплаты</b> — это автоматический механизм перевода прибыли пользователю без необходимости ручного запроса.

После запуска средств в арбитражную работу система фиксирует время старта.

<b>Далее алгоритм работает следующим образом:</b>

<b>1️⃣</b> Через 24 часа система автоматически проверяет баланс пользователя.

<b>2️⃣</b> Если сумма накопленной прибыли составляет 10 USDT и более, то:

— вся заработанная сумма отправляется на заранее сохранённый пользователем адрес USDT (TON).

<b>3️⃣</b> Если за первые 24 часа прибыль менее 10 USDT, то:

— выплата не производится;
— система выполняет повторную проверку ровно через следующие 24 часа;
— при достижении порога вся накопленная прибыль будет отправлена одной транзакцией.

⚙️ <i>Проверки и выплаты выполняются автоматически, без участия пользователя.</i>"""
    
    photo = FSInputFile(get_random_pic())
    await message.answer_photo(
        photo=photo,
        caption=autopay_text,
        reply_markup=get_investor_menu(),
        parse_mode='HTML'
    )
    
    await loading_msg.delete()


@dp.message(F.text == "Как сохранить адрес?")
async def save_address_handler(message: types.Message):
    loading_msg = await message.answer("Загружаем содержимое...")
    
    save_address_text = """💼 <b>Как сохранить адрес для авто-выплат</b>

<b>1️⃣</b> Перейдите в раздел <b>Баланс</b>
<b>2️⃣</b> Пролистайте страницу вниз до блока «Авто-выплаты по балансу»
<b>3️⃣</b> Введите адрес для выплат USDT (TON)
  <i>MEMO — опционально</i>
<b>4️⃣</b> Нажмите кнопку «Сохранить»

<i>После сохранения указанный адрес будет использоваться для авто-выплат.</i>

🔁 <b>Как изменить адрес</b>

Вы можете изменить адрес в любой момент:
перейдите в <b>Баланс</b>, введите новый актуальный адрес и нажмите «Изменить»."""
    
    photo = FSInputFile(get_random_pic())
    await message.answer_photo(
        photo=photo,
        caption=save_address_text,
        reply_markup=get_investor_menu(),
        parse_mode='HTML'
    )
    
    await loading_msg.delete()


@dp.message(F.text == "Об арбитражной модели")
async def arbitrage_model_handler(message: types.Message):
    arbitrage_text_1 = """📊 <b>Об арбитражной модели ARB Core</b>

В основе работы <b>ARB Core</b> лежит <b>синхронный арбитраж</b> — модель, при которой сделки совершаются одновременно на нескольких биржах с целью фиксации ценового расхождения.

⚙️ <b>Как работает синхронный арбитраж</b>

Система отслеживает расхождения цен между биржами в реальном времени и при появлении подходящего спреда:

• одновременно отправлят активы в двух токенах на биржи по API;
• фиксирует разницу при получении активов на биржи;
• мгновенно, одновременно совершает SWAP токенов на биржах;
• завершает цикл сразу после исполнения сделок.

<i>Доход формируется за счёт спреда между биржами, а не за счёт направления рынка.</i>"""
    
    arbitrage_text_2 = """🔍 <b>Полная прозрачность операций</b>

Во время работы арбитражных сделок пользователь в реальном времени видит:

🔹 задействованную торговую пару (например, USDT / SOL)
🔹 биржи, участвующие в арбитраже
🔹 величину спреда между биржами
🔹 хэши транзакций в обеих монетах пары по каждой из задействованных бирж
🔹 хэш транзакции прибыли, возвращённой с бирж

<i>Каждая операция подтверждается блокчейном и доступна для самостоятельной проверки пользователем.</i>

🧠 <b>Принцип модели</b>

• используются только два перевода по API, без ручной торговли
• средства заранее распределены в обоих активах торговой пары
• активы одновременно отправляются на задействованные биржи
• выполняется одновременный SWAP по текущим рыночным условиям
• доход формируется за счёт зафиксированного ценового расхождения

<b>ARB Core не скрывает механику работы</b> — пользователь видит каждую стадию сделки, от входа до возврата прибыли.

<i>Подробно и детально модель развёрнута внутри личного кабинета пользователя и FAQ.</i>"""
    
    await message.answer(
        arbitrage_text_1,
        parse_mode='HTML'
    )
    await message.answer(
        arbitrage_text_2,
        reply_markup=get_investor_menu(),
        parse_mode='HTML'
    )


@dp.message(F.text == "🎥 Видео-материалы")
async def video_materials_handler(message: types.Message):
    loading_msg = await message.answer("Загружаем содержимое...")
    
    video_text = """🎥 <b>Видео материалы</b>

В этом разделе собраны видео-инструкции для инвесторов, которые помогут быстро разобраться в работе с платформой.

<b>📌 Видео 1. Начало работы инвестора</b>
Пошаговый разбор процесса:
• пополнение баланса (депозит);
• запуск средств в работу;
• настройка адреса для авто-выплат;
• получение прибыли.

<b>📌 Видео 2. Вывод партнёрских вознаграждений</b>
Подробная инструкция по:
• просмотру начисленных партнёрских выплат;
• оформлению вывода средств;
• получению партнёрского дохода.

⬇️ <i>Выберите нужное видео ниже и следуйте инструкции.</i>"""
    
    await message.answer(
        video_text,
        reply_markup=get_video_menu(),
        parse_mode='HTML'
    )
    
    await loading_msg.delete()


@dp.message(F.text == "🎬 Видео-материалы")
async def partner_video_materials_handler(message: types.Message):
    loading_msg = await message.answer("Загружаем содержимое...")
    
    video_text = """🎬 <b>Видео материалы для партнёра</b>

В этом разделе собраны видео-инструкции для партнёров, которые помогут разобраться в партнёрской системе.

⬇️ <i>Выберите нужное видео ниже и следуйте инструкции.</i>"""
    
    await message.answer(
        video_text,
        reply_markup=get_partner_video_menu(),
        parse_mode='HTML'
    )
    
    await loading_msg.delete()


@dp.message(F.text == "Вывод из партнёрской системы")
async def partner_withdrawal_video_handler(message: types.Message):
    loading_msg = await message.answer("Загружаем содержимое...")
    
    video_caption = """<b>🎥 Вывод из партнёрской системы</b>

Подробная инструкция по:
• оформлению вывода средств;
• получению партнёрского дохода."""
    
    video = FSInputFile("media/withdrawal.mp4")
    await message.answer_video(
        video=video,
        caption=video_caption,
        reply_markup=get_partner_video_menu(),
        parse_mode='HTML',
        supports_streaming=True,
        width=576,
        height=1280
    )
    
    await loading_msg.delete()


@dp.message(F.text == "Как начать работу?")
async def start_work_video_handler(message: types.Message):
    loading_msg = await message.answer("Загружаем содержимое...")
    
    video_caption = """<b>🎥 Начало работы инвестора</b>

Пошаговый разбор процесса:
• пополнение баланса (депозит);
• запуск средств в работу;
• получение прибыли;
• настройка адреса для авто-выплат.
"""
    
    video = FSInputFile("media/startToWork.mp4")
    await message.answer_video(
        video=video,
        caption=video_caption,
        reply_markup=get_video_menu(),
        parse_mode='HTML',
        supports_streaming=True,
        width=576,
        height=1280
    )
    
    await loading_msg.delete()


@dp.message(F.text == "Как совершить партнёрский вывод?")
async def withdrawal_video_handler(message: types.Message):
    loading_msg = await message.answer("Загружаем содержимое...")
    
    video_caption = """<b>🎥 Вывод партнёрских вознаграждений</b>

Подробная инструкция по:
• оформлению вывода средств;
• получению партнёрского дохода."""
    
    video = FSInputFile("media/withdrawal.mp4")
    await message.answer_video(
        video=video,
        caption=video_caption,
        reply_markup=get_video_menu(),
        parse_mode='HTML',
        supports_streaming=True,
        width=576,
        height=1280
    )
    
    await loading_msg.delete()


@dp.message(F.text == "Инвестору")
async def investor_handler(message: types.Message):
    user_context[message.from_user.id] = 'investor'
    loading_msg = await message.answer("Загружаем содержимое...")
    
    investor_text = """💼 <b>Инвестору | ARB Core INFO</b>

<b>ARB Core</b> — это арбитражный ресурс, ориентированный на системную работу с капиталом в рамках выстроенной операционной модели.

<b>В этом разделе вы получите:</b>
📊 информацию об арбитражной модели и источниках доходности
⚙️ описание логики операций и распределения средств
🧠 понимание роли инвестора в системе
🔍 обзор подходов к управлению рисками
🎥 видео-материалы с поэтапным разбором процессов

⬇️ <i>Используйте кнопки ниже, чтобы последовательно изучить материалы.</i>"""
    
    photo = FSInputFile("media/investorPic.png")
    await message.answer_photo(
        photo=photo,
        caption=investor_text,
        reply_markup=get_investor_menu(),
        parse_mode='HTML'
    )
    
    await loading_msg.delete()


@dp.message(F.text == "◀️ Назад")
async def back_handler(message: types.Message):
    loading_msg = await message.answer("Загружаем содержимое...")
    
    context = user_context.get(message.from_user.id, 'investor')
    
    if context == 'partner':
        partner_text = """🤝 <b>Партнёру | ARB Core INFO</b>

<b>ARB Club</b> — это партнёрская экосистема ресурса <b>ARB Core</b>, ориентированная на развитие сообщества и распределение вознаграждений.

<b>В этом разделе вы получите:</b>
👥 информацию о партнёрской программе
📊 описание структуры и уровней
💰 механику кэшбэка и вознаграждений
🏆 ранговую систему развития
🎯 информацию о мероприятиях и маркетинговой поддержке

⬇️ <i>Выберите интересующий вас раздел ниже.</i>"""
        
        photo = FSInputFile("media/partnerPic.png")
        await message.answer_photo(
            photo=photo,
            caption=partner_text,
            reply_markup=get_partner_menu(),
            parse_mode='HTML'
        )
    else:
        investor_text = """💼 <b>Инвестору | ARB Core INFO</b>

<b>ARB Core</b> — это арбитражный ресурс, ориентированный на системную работу с капиталом в рамках выстроенной операционной модели.

<b>В этом разделе вы получите:</b>
📊 информацию об арбитражной модели и источниках доходности
⚙️ описание логики операций и распределения средств
🧠 понимание роли инвестора в системе
🔍 обзор подходов к управлению рисками
🎥 видео-материалы с поэтапным разбором процессов

⬇️ <i>Используйте кнопки ниже, чтобы последовательно изучить материалы.</i>"""
        
        photo = FSInputFile("media/investorPic.png")
        await message.answer_photo(
            photo=photo,
            caption=investor_text,
            reply_markup=get_investor_menu(),
            parse_mode='HTML'
        )
    
    await loading_msg.delete()


@dp.message(F.text == "🏠 Меню")
async def menu_handler(message: types.Message):
    loading_msg = await message.answer("Загружаем содержимое...")
    
    welcome_text = f"""💙 <b>Добро пожаловать в ARB Core INFO, {message.from_user.first_name}</b>

<b>ARB Core INFO</b> — это информационный центр арбитражного ресурса <b>ARB Core</b>, созданный для тех, кто рассматривает партнёрство или инвестиционное участие в системе арбитражных операций.

<b>Через этого бота вы получите:</b>
📊 структурированную информацию о модели арбитража
🎥 видео-материалы с объяснением процессов
📄 прозрачное описание возможностей
🔐 понимание принципов работы

<b>Выберите интересующий вас формат взаимодействия:</b>

🤝 <b>Партнёру</b> — если вы являетесь или планируете стать активным партнёром экосистемы.

💼 <b>Инвестору</b> — если вы рассматриваете размещение капитала и хотите разобраться в механике доходности и стратегии ресурса.

⬇️ <i>Нажмите нужную кнопку ниже, чтобы продолжить знакомство с ARB Core.</i>"""
    
    photo = FSInputFile("media/welcomePic.png")
    await message.answer_photo(
        photo=photo,
        caption=welcome_text,
        reply_markup=get_main_menu(),
        parse_mode='HTML'
    )
    
    await loading_msg.delete()


async def main():
    await dp.start_polling(bot)


if __name__ == '__main__':
    asyncio.run(main())

