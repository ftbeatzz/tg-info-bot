# 🤖 Telegram Информационный Бот

Информационный Telegram бот с меню, FAQ и видео материалами.

## 📋 Возможности

- ✅ Приветственное сообщение с меню при команде `/start`
- ✅ Раздел FAQ с множеством вопросов и ответов
- ✅ Раздел "Видео материалы" с обучающими видео
- ✅ Удобная навигация с кнопками "Назад"
- ✅ Inline-клавиатуры для лучшего UX

## 🚀 Установка и запуск

### 1. Установите зависимости

```bash
pip install -r requirements.txt
```

### 2. Создайте бота в Telegram

1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и получите токен бота
4. Скопируйте токен

### 3. Настройте переменные окружения

Создайте файл `.env` в корне проекта:

```
BOT_TOKEN=ваш_токен_от_BotFather
```

### 4. Добавьте видео файлы (опционально)

Создайте папку `media` и поместите туда видео файлы:

```
media/
  ├── tutorial.mp4      # Обучающее видео
  └── demo.mp4          # Демонстрационное видео
```

Затем раскомментируйте и обновите пути в `bot.py`:

```python
# Вместо текстового сообщения:
await callback.message.answer_video(
    FSInputFile("media/tutorial.mp4"),
    caption=video_text
)
```

### 5. Запустите бота

```bash
python bot.py
```

## 📁 Структура проекта

```
tg-bot/
├── bot.py              # Основной файл бота
├── requirements.txt    # Зависимости Python
├── .env               # Переменные окружения (создайте сами)
├── .gitignore         # Игнорируемые файлы для Git
├── README.md          # Документация
└── media/             # Папка для видео и изображений (создайте при необходимости)
```

## 🎯 Как использовать

1. Найдите вашего бота в Telegram
2. Нажмите `/start`
3. Выберите интересующий раздел:
   - **FAQ** - часто задаваемые вопросы с ответами
   - **Видео материалы** - обучающие видео

## ⚙️ Настройка контента

### Добавление новых FAQ вопросов

В файле `bot.py` найдите функцию `get_faq_menu()` и добавьте новую кнопку:

```python
[InlineKeyboardButton(text="Ваш вопрос?", callback_data="faq_your_question")],
```

Затем создайте обработчик:

```python
@dp.callback_query(F.data == "faq_your_question")
async def faq_your_question(callback: CallbackQuery):
    answer = "Ваш ответ здесь"
    await callback.message.edit_text(
        answer,
        reply_markup=get_back_button("faq"),
        parse_mode="HTML"
    )
    await callback.answer()
```

### Добавление новых видео

Аналогично добавьте кнопку в `get_videos_menu()` и создайте обработчик.

## 🛠 Технологии

- **Python 3.8+**
- **aiogram 3.14.0** - современная асинхронная библиотека для Telegram Bot API
- **python-dotenv** - управление переменными окружения

## 📝 Примечания

- Бот использует **Inline-клавиатуры** для лучшего пользовательского опыта
- Все тексты поддерживают **HTML форматирование**
- Структура кода модульная и легко расширяемая
- Для работы с видео используйте формат MP4

## 🆘 Поддержка

Если возникли вопросы или проблемы:

1. Проверьте правильность токена в `.env`
2. Убедитесь, что установлены все зависимости
3. Проверьте логи в консоли при запуске бота

## 📄 Лицензия

Проект создан в образовательных целях.
